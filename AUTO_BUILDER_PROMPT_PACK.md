# AUTO_BUILDER_PROMPT_PACK

## Master universal builder prompt
Rehydrate `XPS_AUTO_BUILDER` from repo canon, connected Drive canon, visible skills, backlog, and scheduler state. Preserve canonical names. Treat the requested system as a target profile. Build the next missing or weakest artifact needed to make the target system rehydratable, schedulable, and maintainable. Classify all findings as verified, inferred, missing, blocked, or quarantined.

## Recursive pass prompt
Run one bounded recursive pass. Audit connector reality first. Select one operating mode, one primary artifact, one verified destination, and one next-step handoff. Stop when the pass is complete or blocked.

## Skill routing prompt
Route the current objective to the minimum sufficient skill subset from `SKILL_ORCHESTRATION_MANIFEST.md`. Prefer `xps-auto-builder`, `xps-universal-orchestrator`, `xps-scheduler-fabric`, and `xps-agent-factory` as the default control layer.

## Target-system assembly prompt
Using the current target profile, assemble the target system pack: system spec, component registry, schedule manifest, scheduler handoff, prompt pack, backlog, runtime ledger, recovery runbook, release runbook, and any target-specific connectors or validators.
